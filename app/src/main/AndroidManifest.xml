<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.ac.yjc.wdj.capstondesignproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".StudentGradeManagementList"
            android:screenOrientation="portrait">       <!-- 화면 세로 고정 속성, 모든 .java 파일에 넣어줘야함 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SignupSelectActivity"
            android:screenOrientation="portrait">
        </activity>
        <activity android:name=".FindPWActivity"></activity>
        <activity android:name=".StudentSignup_1"
            android:exported="true"></activity>
        <activity android:name=".StudentSignup_2"></activity>
        <activity android:name=".StudentSignup_3"></activity>
        <activity android:name=".StudentMain"></activity>

    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

</manifest>